<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <menuitem 
        id="booking_menu_main"
        name="Booking" 
        parent="sale.sale_menu_root" 
        sequence="0"/>

        <!-- This Menu Item Must have a parent -->
        <!-- omenuc (odoo menu category) untuk membuat category -->
        <menuitem 
        id="booking_order_menu_categ"
        name="Booking Order" 
        parent="booking_menu_main" 
        sequence="1" 
        action="booking_order_action"/>


        <!-- This Menu Item Must have a parent -->
        <!-- omenuc (odoo menu category) untuk membuat category -->
        <menuitem 
        id="work_order_menu_categ"
        name="Work Order" 
        parent="booking_menu_main" 
        sequence="2" 
        action="work_order_action"/>

        <!-- This Menu Item Must have a parent -->
        <!-- omenuc (odoo menu category) untuk membuat category -->
        <menuitem 
        id="service_team_menu_categ"
        name="Service Team" 
        parent="booking_menu_main" 
        sequence="3" 
        action="service_team_action"/>
    </data>
</odoo>